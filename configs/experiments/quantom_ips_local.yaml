experiment_name: quantom_ips_local
output_dir: logs/quantom_ips_local

app_cfg:
  target: quantom_ips
  module: quantom_ips.drivers.training_workflow
  launcher: python
  vendor: applications

  hydra_overrides:
    environment.parser.path:
      - applications/quantom-ips/data/data.npy
    device: cuda
    dtype: float32
